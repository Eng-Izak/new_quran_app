import 'package:flutter/material.dart';

class ScaleKitBuilder extends StatelessWidget {
  const ScaleKitBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
